---
title: "report"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{report}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(tbr)
data(dat)
data(vpa_result)
```
# 背景
```{r}
nrow(iris)
```

# 目的
```{r}
plot(iris)
```

# 自作関数
```{r}
my_nrow(iris)
```

# データ
```{r}
head(dat$caa) %>% knitr::kable()
head(dat$waa) %>% knitr::kable()
head(dat$maa) %>% knitr::kable()
```

# VPA
```{r}
years <- as.numeric(colnames(vpa_result$ssb))
ssb   <- colSums(vpa_result$ssb, na.rm = TRUE)
plot(years, ssb, type = "b")
```
